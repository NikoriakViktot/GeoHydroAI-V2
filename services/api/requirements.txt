# ── веб/фреймворк
fastapi==0.116.1
uvicorn[standard]==0.35.0
# Не пінь starlette окремо: fastapi підтягне сумісну версію
orjson>=3.10.0
requests

# ── конфіг
pydantic>=2.7
pydantic-settings>=2.4
python-dotenv>=1.0.0

# ── Postgres / SQL
SQLAlchemy>=2.0
psycopg[binary]>=3.1.12
geoalchemy2>=0.14
asyncpg>=0.27.0

# ── гео/растри
shapely>=2.0.0
pyproj>=3.4.0
fiona>=1.9.0
geopandas>=0.14.0
pandas>=2.1.0
numpy>=1.24.0
rasterio>=1.4.0

# ── файли/аналітика
duckdb>=0.9.0
pyarrow>=11.0.0

# ── інше (опційно: векторні БД, аплоад)
pgvector>=0.2.0
python-multipart>=0.0.9
scikit-learn==1.6.1



# ── міграції (коли дійдемо до схеми/seed)
alembic>=1.13.0

# ── валідація датафреймів (корисно для суворих API)
pandera>=0.20.0

# ── AWS S3 (якщо читатимемо parquet/COG зі сховища)
boto3>=1.34.0
